interface:
  display_name: "TrainingPeaks Workout Builder"
  short_description: "Plan, batch-update, and validate TrainingPeaks workouts"
  default_prompt: "Use $trainingpeaks-workout-builder to efficiently plan or update my TrainingPeaks workouts, skip unchanged items, and verify every pace, duration, and distance."

policy:
  allow_implicit_invocation: true
