interface:
  display_name: "TrainingPeaks Workout Builder"
  short_description: "Build and validate structured TrainingPeaks workouts"
  default_prompt: "Use $trainingpeaks-workout-builder to build or update my TrainingPeaks workouts and verify every pace, duration, and distance."

policy:
  allow_implicit_invocation: true
