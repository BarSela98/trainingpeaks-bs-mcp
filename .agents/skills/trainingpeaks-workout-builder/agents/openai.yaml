interface:
  display_name: "TrainingPeaks Workout Builder"
  short_description: "Build, repair, and validate TrainingPeaks workouts"
  default_prompt: "Use $trainingpeaks-workout-builder to build or repair my TrainingPeaks workouts and verify every pace range, duration, and distance."

policy:
  allow_implicit_invocation: true
